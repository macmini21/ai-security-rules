# Copilot 安全指令（工作区级）

本项目的所有AI辅助开发必须遵循以下安全规范。

## 项目安全要求

### 凭据管理
- 所有API密钥、Token、密码必须存储在环境变量或 `.env` 文件中
- `.env` 文件已在 `.gitignore` 中排除
- 代码中禁止出现任何明文凭据
- 配置文件示例仅包含占位符

### 脚本安全
- Python脚本必须验证所有外部输入
- Shell脚本开头使用 `set -euo pipefail`
- 文件操作必须检查路径合法性
- 网络请求必须设置超时
- 子进程调用禁止 `shell=True`

### 依赖安全
- 使用固定版本号
- 定期检查已知漏洞
- 最小化依赖数量

### 日志与调试
- 日志中禁止包含密码、Token、Cookie
- 调试信息在生产环境中禁止输出
- 错误消息不暴露内部实现

### 文件权限
- 包含密钥的文件权限设为 600
- 脚本文件权限设为 755
- 目录权限设为 755

### Git安全
- 提交前确认无敏感信息泄露
- 使用 `.gitignore` 排除所有敏感文件
- 禁止将 `.envs/` 目录下的文件提交到仓库
